class Ldap {
    fun createUser(user:String) {
        // 생성 로직
        println("Ldap 에 user를 등록한다.")
    }
    fun updateUser(user:String) {
        // 퇴사 처리, 휴직 처리 로직
        // 조직 변경 로직
        println("Ldap 에 user를 수정한다.")
    }
    fun createGroup(group:String) {
        // 그룹 생성 로직
        println("Ldap 에 group을 등록한다.")
    }
    fun updateGroup(group:String) {
        // 그룹 수정 로직
        println("Ldap 에 group을 수정한다.")
    }
}